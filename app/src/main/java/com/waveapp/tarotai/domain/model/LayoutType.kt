package com.waveapp.tarotai.domain.model

/**
 * Tipo de disposición visual de las cartas en una tirada.
 */
enum class LayoutType {
    HORIZONTAL,  // Disposición lineal horizontal
    CROSS        // Disposición en forma de cruz (para tirada Cruz)
}
